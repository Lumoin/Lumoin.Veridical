<Project Sdk="MSTest.Sdk">

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <EnableTrimAnalyzer>false</EnableTrimAnalyzer>
    <EnableAotAnalyzer>false</EnableAotAnalyzer>
    <!--
      Displays error on console in addition to the log file. Note that this feature comes with a performance impact.
      For more information, visit https://learn.microsoft.com/dotnet/core/testing/unit-testing-platform-integration-dotnet-test#show-failure-per-test
      -->
    <TestingPlatformShowTestsFailure>true</TestingPlatformShowTestsFailure>
    <RunSettingsFilePath>$(MSBuildProjectDirectory)\config.runsettings</RunSettingsFilePath>
    <TestingExtensionsProfile>AllMicrosoft</TestingExtensionsProfile>
    <IsTestApplication>true</IsTestApplication>
    <MSTestAnalysisMode>Recommended</MSTestAnalysisMode>
    <!--
      The tests mimic an application consuming real credentials and artifacts, so they read
      their JSON fixtures (Wycheproof vectors, predicate-proof envelopes) with System.Text.Json.
      The library itself stays serialization-free and is fed parsed bytes/offsets (or a parse
      delegate) by its caller, so this test project opts out of the serialization ban. Its mdoc
      CBOR goes through Lumoin.Veritas.Cbor, which the ban does not name.
      -->
    <EnableSerializationBan>false</EnableSerializationBan>
  </PropertyGroup>

  <PropertyGroup Condition="'$(VERIDICAL_STRYKER_RUN)' == 'true'">
    <!--
      The mutation runner drives the suite through the VsTest host, so the
      MSTest SDK's own test host and its dotnet test integration are switched
      off for that build only. That build restores outside the package lock
      file because the VsTest host brings in a package the ordinary build
      does not reference. The lock-file path is redirected to a location
      under the intermediate output directory because NuGet refuses a
      disabled lock file that still exists at its resolved path, and nothing
      is ever written there. The testing-extensions profile is cleared
      because the VsTest targets reject a non-empty profile.
      -->
    <UseVSTest>true</UseVSTest>
    <EnableMSTestRunner>false</EnableMSTestRunner>
    <TestingPlatformDotnetTestSupport>false</TestingPlatformDotnetTestSupport>
    <TestingExtensionsProfile></TestingExtensionsProfile>
    <RestorePackagesWithLockFile>false</RestorePackagesWithLockFile>
    <NuGetLockFilePath>$(BaseIntermediateOutputPath)stryker-packages.lock.json</NuGetLockFilePath>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CsCheck" />
    <PackageReference Include="Lumoin.Veritas.Cbor" />
    <PackageReference Include="LiquidTestReports.Markdown" />
    <PackageReference Include="Microsoft.Extensions.TimeProvider.Testing" />
    <PackageReference Include="ModelContextProtocol" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Core\Lumoin.Veridical.Core.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Bbs\Lumoin.Veridical.Bbs.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Hashing\Lumoin.Veridical.Hashing.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Analysis\Lumoin.Veridical.Analysis.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Backends.Managed\Lumoin.Veridical.Backends.Managed.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Longfellow\Lumoin.Veridical.Longfellow.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Secdsa\Lumoin.Veridical.Secdsa.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Json\Lumoin.Veridical.Json.csproj" />
    <ProjectReference Include="..\..\src\Lumoin.Veridical.Cli\Lumoin.Veridical.Cli.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Update="Spartan\Fixtures\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="ConstraintSystems\Interop\Circom\Fixtures\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="ConstraintSystems\Interop\ZkInterface\Fixtures\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="Cryptography\Wycheproof\Fixtures\**\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
